/**
 * Config Port Interface
 *
 * Re-exports the IConfigPort from the config module for consistency.
 */

export type { IConfigPort } from "../config/config-service.js";
export type { AppConfig, BrowserOptions, ConfigOverrides } from "../config/index.js";
